# Math Curve Loaders Example

Manual visual tester for the `math_curve_loaders` package.

The example app shows every loader preset in a compact gallery and opens each
one in a focused preview with shared visual controls, reduced-motion simulation,
theme switching, and a generated Dart snippet.

Run it locally:

```sh
fvm flutter pub get
fvm flutter run -d macos
```
