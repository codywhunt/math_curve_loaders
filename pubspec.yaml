name: math_curve_loaders
description: Animated Flutter loading indicators drawn from parametric mathematical curves.
version: 0.1.0
repository: https://github.com/codywhunt/math_curve_loaders
issue_tracker: https://github.com/codywhunt/math_curve_loaders/issues
topics:
  - flutter
  - loading
  - animation
  - math
  - curves

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
